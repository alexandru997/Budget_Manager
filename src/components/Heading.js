const Heading = ({ text }) => {
    return <h1 className='display-1 mt-3'>{text}</h1>
}

const Title = ({ text }) => {
    return <h2 className='display-6 mt-3'>{text}</h2>
}

export { Title }
export default Heading
